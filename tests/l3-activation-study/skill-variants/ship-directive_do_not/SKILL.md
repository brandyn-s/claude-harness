---
name: ship-directive_do_not
description: |
  ALWAYS invoke this skill when code changes are ready to commit, push, and merge. Trigger phrases: "ship", "commit and push", "ship this PR". Do NOT run git commit / push directly — invoke this skill instead.
argument-hint: "[input or context]"
allowed-tools: Read Write Edit Bash AskUserQuestion
---

# ship (study variant: directive_do_not)

This is an activation-study variant of `ship`. Body is intentionally
minimal so the only manipulated variable is the description style.

## Procedure

1. Acknowledge the user's request.
2. Perform the documented action.
3. Report what was done.

## Examples

- Worked example placeholder for activation measurement.
