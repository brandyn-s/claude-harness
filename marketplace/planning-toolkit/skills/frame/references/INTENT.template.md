# Intent — <task name>

*Written <YYYY-MM-DD HH:MM> by <human> with <model>. One screen. Update it; do not append prose.*

## Problem
<One paragraph in the user's words. What is wrong or missing, for whom, and how you would notice it was fixed. No solution here.>

## Done when
- [ ] `<command>` passes — <what it proves>
- [ ] <observation a stranger could re-run, with the number in it>
- [ ] <the user-facing outcome, seen through the real entry point>

## Non-goals
- <what will not be built this time, even though it is nearby>
- <the tempting refactor that stays untouched>

## Constraints
- <how it must be done: language/version, data boundaries, no new dependencies, …>

## Riskiest assumption
<one sentence> — probe: `<the cheapest check that would refute it>`, scheduled first.

## Stays human
- <decision the model brings back rather than makes: interface shape, deletions, external writes, …>

## Model / effort
<model> at <effort>; <one line on why — see model_notes>.

## Revisions
- <YYYY-MM-DD HH:MM> — <what changed and why>
