id: sync-repo
type: hook
# NOT A REGISTERED HOOK. sync-repo.py is a CLI
# (`python sync-repo.py --git-status|--pull`) invoked by session-start modules and
# by hand, not registered on any hook event. event/matcher are null so graph
# queries cannot report it as part of any enforcement chain.
event: null
matcher: null
if_condition: null
action_type: guard
description: "Refresh and report status of local clones of managed Example repos."
enforces: []  # TODO: which rules does this hook enforce?
blocks_patterns: []  # TODO: what does it block?
injects: null  # TODO: what context does it inject?
modifies: null
depends_on_files: []
depends_on_env: []
exit_codes:
  "0": "allow"
  "2": "block"  # TODO: verify
output_format: json
incidents: []
