# Audit suppressions for api-preflight.
# See skills/audit-skill/SKILL.md for the schema.
#
# All suppressions below relate to the constraint_graph.py cluster.
# constraint_graph.py lives at ~/Documents/api-docs/constraint_graph.py.
# /api-ingest BOOTSTRAPS a minimal stub of it on first ingest (Phase 4.5a)
# and /api-preflight only READS it (Phase 0 --chain). Both skills invoke it
# (api-ingest with --dump for parse verification; api-preflight with --chain
# for the fast-path query). api-preflight Phase 0 reads the script if present
# and skips cleanly (exit 0) when absent rather than hard-aborting.
suppressions:
  - code: A1
    target: "Phase 0 bash hard-abort"
    reason: |
      Reproducer hard-codes the OLD Phase 0 bash block (with `exit 1`). The
      current SKILL.md Phase 0 emits a "Note:" message to stderr and falls
      through to Phase 1 with exit 0 — no hard abort. The bug pattern
      described in the finding is no longer present in SKILL.md.

  - code: D4
    target: "constraint_graph.py producer (cross-skill drift)"
    reason: |
      Cross-skill drift is now RECONCILED. api-ingest Phase 4.5a bootstraps
      a minimal stub of constraint_graph.py on first ingest; api-preflight
      Phase 0 prose now states this directly and frames itself as a
      read-only consumer (query if present, skip if absent). The earlier
      "NOT generated by /api-ingest" claim has been corrected — both skills
      now agree that /api-ingest bootstraps and /api-preflight reads.

  - code: D4
    target: "/root/.claude/agent-memory/topics/microsoft-graph.md"
    reason: |
      The skill no longer documents a literal substitution path. Phase 1
      Step 3 (SKILL.md lines 200-208) prescribes an alias fallback chain
      (microsoft-graph -> msgraph, google-workspace -> gworkspace, etc.)
      followed by a `grep -l` discovery step, followed by mcp__memory-search
      as the canonical lookup. Example 1 has been rewritten to show the
      msgraph.md alias resolution explicitly. The file_missing reproducer
      tests a literal path that the documented procedure never resolves to.

  - code: D4
    target: "Example 2 slack groups:read claim"
    reason: |
      The fabricated "groups:read needed (bot tokens), not channels:read"
      claim has been removed from Example 2. The example now cites the
      actual [confirmed] [tool-gotcha] entry from slack.md (2026-03-04/05)
      about per-user OAuth tokens losing channels:read, with the documented
      workaround (re-authorize, or use conversations_search_messages). The
      reproducer greps slack.md for a claim that is no longer in api-preflight
      to be matched.

  - code: D4
    target: "gotchas.md reader expectation"
    reason: |
      Important Rules (SKILL.md line 409) now explicitly states that
      /api-ingest does NOT emit gotchas.md and that operational issues
      live in agent-memory topic files instead. The reproducer greps
      api-ingest for gotchas.md writes — the absence is now correctly
      reflected in api-preflight's reader-side prose. Cross-skill update
      to api-ingest is out of scope.

  - code: D1
    target: "constraint_graph.py cross-component contract"
    reason: |
      Both consumers (api-ingest --dump, api-preflight --chain) call into
      ~/Documents/api-docs/constraint_graph.py. api-ingest Phase 4.5a
      bootstraps a minimal stub of it on first ingest; api-preflight Phase 0
      reads it if present and treats its absence as a clean precondition
      skip (exit 0). The two skills are now aligned on this producer/consumer
      contract — api-ingest bootstraps, api-preflight reads.

  - code: G1
    target: "/root/.claude/agent-memory/topics/microsoft-graph.md"
    reason: |
      Same root cause as the D4 microsoft-graph.md finding above. Example 1
      now narrates the alias-fallback resolution (microsoft-graph -> msgraph)
      explicitly, and Phase 1 Step 3 documents the fallback chain. The
      file_missing predicate tests a literal path no documented procedure
      resolves to.

  - code: A3
    target: "constraints.md hand-curated claim"
    reason: |
      Source-priority tier 4 in SKILL.md now reads "structured,
      auto-generated by /api-ingest from OpenAPI specs + Firecrawl
      extraction (no hand-curation pass)". The "hand-curated" framing the
      finding flagged has been removed. The reproducer confirms api-ingest
      writes constraints.md programmatically — which the updated prose now
      correctly acknowledges.

  - code: B
    target: "Phase 0 error path recovery hint"
    reason: |
      Phase 0 no longer emits "Error: ... Run /api-ingest <name> first.".
      It emits a "Note: constraint_graph.py not found at $GRAPH — skipping
      Phase 0." message to stderr and continues. The misleading
      /api-ingest recovery hint has been removed entirely. The reproducer
      tests api-ingest for self-installation language — which neither
      skill claims to do.

  - code: F3
    target: "Mode detection consistency"
    reason: |
      SKILL.md Mode Detection section now enumerates trigger phrase lists
      for both Troubleshooting and Pre-coding modes (HTTP error codes,
      auth-failure phrases, symptom language, diagnostic verbs vs. action
      verbs, planning language) and includes an AskUserQuestion fallback
      when both lists match or neither matches. The original [unverified]
      recommendation (enumerate triggers, or ask user when ambiguous) is
      now implemented as Mode Detection's Decision Rule steps 1-4.
