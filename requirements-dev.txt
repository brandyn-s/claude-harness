# Test + lint dependencies. Install with:
#   pip install -r requirements-dev.txt
#
# These are the libraries the hook scripts and tests import at runtime
# beyond the Python stdlib. CI installs them in .github/workflows/tests.yml.

pyyaml        # hooks/rule_context_budget.py, manifests/compile.py
openpyxl      # hooks/xlsx-to-md.py + test_xlsx_to_md.py
pytest        # hooks/test-hooks/, skills/*/tests/, scripts/test_chatgpt.py
requests      # scripts/chatgpt.py + scripts/test_chatgpt.py
ruff~=0.16.5  # scripts/test_ruff_clean.py lint gate; pinned to the 0.16 line the repo was cleaned under
